{
  "scripts": {
    "start": "npx -y live-server docs",
    "prestart": "npm run build:web",
    "build:web": "emcmake cmake -B wbuild -G Ninja && cmake --build wbuild",
    "build:native": "cmake -B build -G Ninja && cmake --build build"
  }
}
